# lute.min.js

此目录下的 `lute.min.js` / `lute.min.js.map` 为构建产物，已加入 `.gitignore`，不纳入 office 仓库版本管理。

在同级 `my_lute` 仓库中执行：

```bash
cd ../../my_lute/javascript
./build-vscode-office.sh
```

构建 vditor 时若文件缺失，会自动尝试调用上述脚本。
